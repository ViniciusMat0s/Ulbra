import math #Importando o módulo para cálculos

num = 20 #Atribuindo um valor

raiz_quadrada = math.sqrt(num) #Variável recebe o valor da raiz quadrada

print(f"A raiz quadrada de {num} é {raiz_quadrada}")